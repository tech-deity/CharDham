<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginBottom="2dp"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    app:cardElevation="2dp"
    android:layout_height="100dp">

<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/placeImage"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_toEndOf="@+id/placeName"
        android:scaleType="fitXY"
        android:src="@drawable/default_image"
       />

    <View
        android:layout_alignStart="@+id/placeImage"
        android:layout_alignEnd="@+id/placeImage"
        android:alpha="0.4"
        android:background="@color/black_252525"
        android:layout_alignBottom="@+id/placeImage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/placeName"
        android:layout_width="250dp"
        android:layout_height="match_parent"
        android:fontFamily="sans-serif-condensed"
        android:maxLines="2"
        android:elevation="8dp"
        android:gravity="center"
        android:background="@color/white"
        android:textAlignment="center"
        android:text="Gangotari and Yamnotri"
        android:textColor="@color/orange"
        android:textSize="18sp"
        android:textStyle="bold"
        />

    <ImageView
        android:id="@+id/wish_fav_iv"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:layout_alignStart="@+id/placeImage"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:src="@drawable/baseline_favorite_border_24"
        android:tint="@color/white" />
</RelativeLayout>
</androidx.cardview.widget.CardView>
